//package med.Vol.api.dominio.usuarios;

//public record listaDatosUsuarios(boolean admin,Long id, String nombre, String correo, String telefono) {
//    public listaDatosUsuarios(Usuario usuario){
//        this(usuario.getAdmin(), usuario.getId(), usuario.getNombre(), usuario.getCorreo(), usuario.getTelefono());
//    }
//}

